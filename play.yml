---
- hosts : all  # since we need to run on all host
  become : yes   # already user is sudo user so using its sudo privilege
  tasks :
  - name : i wnat to install webserver on nodes
    apt : 
    - name : apache2
      state : present 
      